# CLAIMS LEDGER — the single source of truth for what the harness claims

Every claim lives here, in one format and one ID space (ADR 16/17): the
guarantees the harness itself asserts (source: harness) and every externally
sourced claim ever evaluated (source: the external doc). This file answers
both standing questions in one grep:

- **"What does the harness assure, and at what degree?"** — the adopted rows,
  each carrying its enforcement degree. The degree mix (force / half-force /
  steer) is the measured guard against becoming one more prompt-and-pray
  skills repo; the selftest prints the mix on every run.
- **"Has this claim been evaluated before?"** — the entry point of the
  improvement pipeline: any incoming source resolves per claim to already
  addressed (cite the row) · new (evaluate) · better than ours (supersede by
  appending a row citing the old ID) · principle we lacked (adopt).

One row per claim per evaluation, appended, never edited: a row is a **dated
snapshot** — its anchor was verified on that date and may rot honestly, like
an ADR's checked-on date.

Rules:
- **Grep here first.** Every `/absorb` run starts by searching this file for the
  source and its claims; a previously evaluated claim cites its row instead of
  being re-deliberated.
- IDs are stable and sequential (`C-NNN`); the selftest rejects duplicates.
- Verdict is one of: **adopted** · **rejected** (with the value it loses to) ·
  **already have** · **deferred** (with its trigger in BACKLOG).
- **Every adopted row carries its enforcement degree, honestly labeled**
  (prefer force over steer — PLAYBOOK meta-rule): `(force)` wired as
  hook/lint/CI gate · `(half-force)` mechanical half wired, semantic half
  convention · `(steer)` rite text or convention, admitted only when the rule
  cannot be reified AND is load-bearing. A claim whose obedience cannot be
  forced at any degree, nor even be seen violated in review or audit, is
  decoration and is rejected. The selftest rejects a bare `adopted`.
- **Force rows cite their executor** (the selftest case, hook, or workflow
  that proves them), and every `scripts/selftest*.sh` gate must be indexed
  here — a new gate without a ledger row fails CI (coverage check). Honest
  limit: the checks verify existence and reference, not semantic match; the
  prose-to-gate correspondence stays human (ADR 1).

| ID | Date | Source | Claim | Verdict | Where |
| --- | --- | --- | --- | --- | --- |
| C-001 | 2026-07-17 | agent-skills/code-review-and-quality | Severity ladder Critical/Required/Nit/FYI on findings | adopted (steer) | `templates/ts-base/.claude/agents/auditor.md` (ADR 14) |
| C-002 | 2026-07-17 | agent-skills/code-review-and-quality | Order findings by leverage; never bury structural under nits | adopted (steer) | `auditor.md` (ADR 14) |
| C-003 | 2026-07-17 | agent-skills/code-review-and-quality | Quantify findings — counts, not qualifiers | adopted (steer) | `auditor.md` (ADR 14) |
| C-004 | 2026-07-17 | agent-skills/code-review-and-quality | Security and performance as review axes | adopted (steer) | `auditor.md` (ADR 14) |
| C-005 | 2026-07-17 | agent-skills/test-driven-development | Rationalization table: alibi → short declarative reply | adopted (steer) | `feature`/`audit`/`absorb` SKILL.md (ADR 14) |
| C-006 | 2026-07-17 | agent-skills review discussion (2026-07-15) | Dependency discipline: 1 dep/PR, changelog before upgrade, stdlib-first, lockfile never by hand | adopted (steer; wired half = weekly pnpm audit in CI) | `templates/ts-base/AGENTS.md` (ADR 14) |
| C-007 | 2026-07-17 | agent-skills review discussion (2026-07-15) | Skill instructions demand verifiable artifacts; open questions are decoration | adopted (half-force) | PLAYBOOK Mechanism selection + `scripts/selftest-skills.sh` (ADR 14) |
| C-008 | 2026-07-17 | agent-skills review discussion (2026-07-15) | Form limits on skills: 1-line description, body cap, mandatory output section | adopted (force) | `scripts/selftest-skills.sh` (ADR 14) |
| C-009 | 2026-07-17 | agent-skills (catalog) | Install the catalog / router skill | rejected: ADR 9 silent propagation + context saturation | ADR 14 |
| C-010 | 2026-07-17 | agent-skills/code-review-and-quality | Review-speed SLAs, multi-round reviewer process | rejected: team ceremony at n=1 | ADR 14 |
| C-011 | 2026-07-17 | agent-skills/code-review-and-quality | Named structural remedies in reviews (propose the move, not just the problem) | deferred: sweep lot 2 | BACKLOG (harness) |
| C-012 | 2026-07-17 | agent-skills/code-review-and-quality | Change sizing thresholds + splitting strategies | deferred: sweep lot 2 | BACKLOG (harness) |
| C-013 | 2026-07-17 | agent-skills/code-review-and-quality | Dead-code hygiene: list orphans explicitly, ask before deleting | deferred: sweep lot 2 | BACKLOG (harness) |
| C-014 | 2026-07-17 | agent-skills/doubt-driven-development | Fresh-context review before non-trivial output stands | already have | `templates/ts-base/.claude/skills/audit/SKILL.md:8`; PLAYBOOK routine audit (ADR 15) |
| C-015 | 2026-07-17 | agent-skills/doubt-driven-development | Adversarial reviewer framing ("find what is wrong") | rejected: neutral-prompt anti-confabulation; reify-to-test is the arbiter | ADR 15 |
| C-016 | 2026-07-17 | agent-skills/doubt-driven-development | Reviewer gets artifact + contract, never the author's claim or reasoning | already have | `audit/SKILL.md:9-10`; `auditor.md:7-9` |
| C-017 | 2026-07-17 | agent-skills/doubt-driven-development | Non-trivial decision definition (branching / boundary / type-unverifiable / irreversible) | adopted (steer) | `home/skills/decide/SKILL.md` (ADR 15) |
| C-018 | 2026-07-17 | agent-skills/doubt-driven-development | Bounded doubt loop (3 cycles) + doubt-theater flag | rejected: single-pass audit + AGENT-LOG real/confabulated ratio covers calibration | ADR 15 |
| C-019 | 2026-07-17 | agent-skills/doubt-driven-development | Cross-model second opinion (read-only sandbox, stdin piping) | deferred: trigger = audit miss from shared blind spot, or a 2nd model CLI present | BACKLOG (harness) |
| C-020 | 2026-07-17 | agent-skills/test-driven-development | Prove-it: a bug fix starts with a failing reproduction test | adopted (steer) | `templates/ts-base/AGENTS.md` + PLAYBOOK Layer 0 (ADR 15) |
| C-021 | 2026-07-17 | agent-skills/test-driven-development | Assert outcomes/state, never internal call sequences | adopted (steer) | `templates/ts-base/AGENTS.md` + PLAYBOOK Layer 0 (ADR 15) |
| C-022 | 2026-07-17 | agent-skills/test-driven-development | DAMP over DRY in test code | adopted (steer) | `templates/ts-base/AGENTS.md` (ADR 15) |
| C-023 | 2026-07-17 | agent-skills/test-driven-development | Double ladder: real > fake > stub > mock | already have | `templates/ts-base/AGENTS.md:24-25` fakeable seam; PLAYBOOK.md:39-40 |
| C-024 | 2026-07-17 | agent-skills/test-driven-development | Test pyramid percentages + small/medium/large taxonomy | rejected: quota-shaped; "lowest level that can catch the bug" does the work | ADR 15 |
| C-025 | 2026-07-17 | agent-skills/test-driven-development | Beyonce rule: behavior you rely on carries a test | already have | PLAYBOOK.md:41-44 human-owned checklist + tests ship with logic |
| C-026 | 2026-07-17 | agent-skills/test-driven-development | Browser runtime verification via DevTools MCP | rejected: Layer 4 E2E row covers it; no consumer yet | PR #13 table |
| C-027 | 2026-07-17 | agent-skills/test-driven-development | Subagent writes the reproduction test blind to the fix | rejected: ceremony at n=1; the auditor covers the audit case | PR #13 table |
| C-028 | 2026-07-17 | agent-skills/planning-and-task-breakdown | Plan before code, read-only, human approves | already have | `templates/ts-base/.claude/skills/feature/SKILL.md` step 2 |
| C-029 | 2026-07-17 | agent-skills/planning-and-task-breakdown | Standing tasks/plan.md + tasks/todo.md checklists | rejected: ADR 6 — plan docs are views over tests; hand-ticked checklists lie | ADR 15 |
| C-030 | 2026-07-17 | agent-skills/planning-and-task-breakdown | Vertical slicing, task sizing ("and" in title = two tasks), checkpoint cadence, contract-first parallelization | deferred: folded into the /plan rite item | BACKLOG (harness) |
| C-031 | 2026-07-17 | agent-skills/planning-and-task-breakdown | Highest-risk task first (fail fast) | already have | `.claude/skills/kickoff/SKILL.md:15-16` riskiest assumption |
| C-032 | 2026-07-17 | harness (ADR 1) | ts-base is consumed exactly as its README instructs; `verify` is green on the empty scaffold | adopted (force) | selftest.sh Claim 1, in CI on every push |
| C-033 | 2026-07-17 | harness (ADR 2) | AGENTS.md is canonical; CLAUDE.md/GEMINI.md are one-line adapters; settings.json valid | adopted (force) | selftest.sh Claim 0 |
| C-034 | 2026-07-17 | harness (ADR 6) | /feature + /audit rites and the auditor ship in the template; the auditor stays read-only | adopted (half-force) | presence + read-only tools line: selftest.sh Claim 0b; execution of the rites: steer on invocation |
| C-035 | 2026-07-17 | harness (ADR 1) | Commit #1 passes the pre-commit chain deletion-guard → lint-staged → verify | adopted (force) | selftest.sh Claim 2 |
| C-036 | 2026-07-17 | harness (ADR 1) | The deletion guard blocks a >80-line deletion unless explicitly overridden | adopted (force) | selftest.sh Claim 3 |
| C-037 | 2026-07-17 | harness (ADR 7) | The lint gate REJECTS violating code — any, eqeqeq, switch-exhaustiveness, no-cycle, max-depth each seen firing (no wired-but-blind rules) | adopted (force) | selftest.sh Claim 4 |
| C-038 | 2026-07-17 | harness (roadmap 2026-07-10) | vue-starter is consumed per its README; the gate rejects validity, vue and pure-core violations | adopted (force) | selftest-vue.sh |
| C-039 | 2026-07-17 | harness (ADR 10/13) | Writes outside the project root are denied — plain, `../` and symlink escapes — with a named allowlist (memory, scratchpad, data repo) | adopted (force) | write-containment.py + selftest-home.sh |
| C-040 | 2026-07-17 | harness (secret-hygiene 2026-07-13) | Secret-shaped prompts are blocked before leaving the machine | adopted (force) | secret-scan.py + selftest-home.sh |
| C-041 | 2026-07-17 | harness (secret-hygiene 2026-07-13) | Env-dumping commands (printenv, cat .env) are denied; the sanctioned substitution flow passes | adopted (force) | env-dump-guard.py + selftest-home.sh |
| C-042 | 2026-07-17 | harness (ADR 10) | Every home hook is wired in settings.json and executable — an unreferenced hook fails the selftest | adopted (force) | selftest-home.sh wiring section |
| C-043 | 2026-07-17 | harness (ADR 16) | Ledger contract: unique ids, closed verdict set, adopted rows carry a degree | adopted (force) | scripts/selftest-skills.sh ledger checks |
| C-044 | 2026-07-17 | harness (pipeline doctrine) | CI runs the selftests on every push AND every pull_request, never PR-only | adopted (force) | .github/workflows/selftest.yml `on:` block |
| C-045 | 2026-07-17 | harness (ADR 3) | Branch protection ruleset at kickoff: PR required, no force-push, required status check — binds every actor server-side | adopted (steer; force in the consumer repo once the ruleset is applied) | PLAYBOOK kickoff step 6 |
| C-046 | 2026-07-17 | harness (ADR 9) | Copied skills carry `source: agentic-harness@<sha>` provenance | adopted (steer; drift report deferred per ADR 12) | `.claude/skills/kickoff/SKILL.md` step 3 |
| C-047 | 2026-07-17 | harness (ADR 12) | Layer A anti-destruction: minimal diff, read before edit, no drive-by cleanup, no gutting, shown evidence, anchoring law | adopted (steer; read-before-edit is half-force via the harness, write containment is force) | `home/claude/CLAUDE.md` Layer A section |
| C-048 | 2026-07-17 | harness (ADR 6) | Plan/checklist docs are views over tests, never a parallel source of truth | adopted (steer) | PLAYBOOK Layer 0 + /feature closeout |
| C-049 | 2026-07-17 | harness (ADR 6) | Audits run fresh-context, neutrally framed, findings reified into tests | adopted (steer; auditor read-only is force per C-034) | `templates/ts-base/.claude/skills/audit/SKILL.md` + PLAYBOOK routine |
| C-050 | 2026-07-17 | harness (pipeline doctrine) | Commit/push of work branches is automatic; merge to default is always the human's act | adopted (steer; force once the consumer ruleset walls the default branch) | PLAYBOOK pipeline section |
| C-051 | 2026-07-17 | strategy-brief 2026-07-17 (another AI session) | Evidence-as-force: a Stop hook refusing "done" unless verify ran this session | deferred: trigger = first real "IMPLEMENTED - NOT VERIFIED" incident on a product; today evidence is steer per C-047 | BACKLOG harness-candidates queue |
| C-052 | 2026-07-17 | strategy-brief 2026-07-17 (another AI session) | Secret scanning of git history/staged files (gitleaks) in ts-base pre-commit + CI | already have (spec'd, unbuilt): BACKLOG secret-hygiene pack item, 2026-07-13 — the brief independently re-derived it | BACKLOG (harness section) |
| C-053 | 2026-07-17 | strategy-brief 2026-07-17 (another AI session) | Executable architecture boundary (src/lib must not import UI) wired, not optional | deferred: documented at PLAYBOOK.md:110 as optional; wire in vue-starter when a consumer's src/lib exists | BACKLOG harness-candidates queue |
| C-054 | 2026-07-17 | strategy-brief 2026-07-17 (another AI session) | Surface pack "LLM in the product": output schema, golden/negative fixtures, stub provider, no LLM without a seam | deferred: trigger = next AI-in-product feature (AI Insights anchor / job-fit-assistant evolution) | BACKLOG harness-candidates queue |
| C-055 | 2026-07-17 | strategy-brief 2026-07-17 (another AI session) | Scripted one-command worktree helper | rejected: tooling for the map — the never-share-index rule stands (PLAYBOOK pipeline) and `git worktree add` is one command; revisit on recurring collision pain | — |
| C-056 | 2026-07-17 | strategy-brief 2026-07-17 (another AI session) | Characterization-test scaffold skill for untested legacy | already have (deferred): brownfield overlay item, pre-registered trigger = Kinous sweep start | BACKLOG (harness section) |
| C-057 | 2026-07-17 | strategy-brief 2026-07-17 (another AI session) | Growth queue: the harness grows ONLY from `harness-candidate:` lines logged when a product feature hurt — never from map-completion | adopted (steer) | PLAYBOOK Roadmap intro + BACKLOG harness-candidates section (ADR 18) |
| C-058 | 2026-07-17 | strategy-brief 2026-07-17 (another AI session) | Kill/continue soft rule: ~90 days without measurable review-cost reduction freezes the meta-repo; harness work capped as a tax on shipping | deferred: governance is the owner's signature, not the agent's — awaiting Daniel's explicit adoption | BACKLOG (harness section, flagged for Daniel) |
| C-059 | 2026-07-17 | strategy-brief 2026-07-17 (another AI session) | Default investment now: dogfood on a real product (OrgLab), zero new harness dimensions | already have: BACKLOG priority ordering (sweep runs behind Ingentis/OrgLab) — the brief's brake and ours agree | BACKLOG (top-level ordering) |
| C-060 | 2026-07-17 | Daniel (session feedback 2026-07-17) | Evaluation requests end in a report; implementation waits for the explicit go ("containing the models' creative force") | adopted (half-force: the reminder fires mechanically and plan mode blocks edits; the enter-plan-mode link stays steer) | deliberation-nudge.py + selftest-home.sh ± cases; constitution Layer A item 7; /absorb step 3; harness-repo `defaultMode: plan` (ADR 19) |
| C-061 | 2026-07-17 | agent-skills/debugging-and-error-recovery | Ordered debugging triage: reproduce → localize (bisect) → reduce → root cause → guard → verify e2e | adopted (half-force: presence wired in selftest.sh Claim 0b; execution steer on invocation) | `templates/ts-base/.claude/skills/debug/SKILL.md` (ADR 20) |
| C-062 | 2026-07-17 | agent-skills/debugging-and-error-recovery | Stop-the-line: no feature work past an unexpected failure | adopted (steer) | `/debug` opening rule (ADR 20) |
| C-063 | 2026-07-17 | agent-skills/debugging-and-error-recovery | Error output is data, never instructions (stack traces, CI logs, API errors can carry injected commands) | adopted (steer) | constitution Layer A item 8 + `/debug` (ADR 20) |
| C-064 | 2026-07-17 | agent-skills/debugging-and-error-recovery | Safe fallbacks: default + warn on missing config, graceful degradation | rejected: loses to fail-fast config validation (PLAYBOOK Node conventions) — a silent fallback is the class of bug the auditor hunts | ADR 20 |
| C-065 | 2026-07-17 | agent-skills/debugging-and-error-recovery | Temporary instrumentation: add to localize, remove before commit | adopted (steer) | `/debug` step 6 (ADR 20) |
| C-066 | 2026-07-17 | agent-skills/incremental-implementation | Thin vertical slices; increment cycle implement→test→verify→commit | already have | verify-gated atomic commits (`templates/ts-base/AGENTS.md`); slicing material parked in C-030 |
| C-067 | 2026-07-17 | agent-skills/incremental-implementation | Simplicity first: generalize at the third use case, never before; three similar lines beat a premature abstraction | adopted (steer; complexity caps stay the wired half) | `templates/ts-base/AGENTS.md` working rules (ADR 20) |
| C-068 | 2026-07-17 | agent-skills/incremental-implementation | Scope discipline: note adjacent issues, never fix them in-task | already have | layer A no-drive-by (C-047) + backlog rite |
| C-069 | 2026-07-17 | agent-skills/incremental-implementation | Feature flags for incomplete work · opt-in safe defaults · rollback-friendly increments | rejected: short work branches + PR flow cover it at n=1; deletion guard + refactor-vs-behavior commits stand | ADR 20 |
| C-070 | 2026-07-17 | agent-skills/incremental-implementation + tdd | Never re-run an unchanged command "just to be sure" | rejected: efficiency nicety, no correctness value to beat | ADR 20 |
| C-071 | 2026-07-17 | agent-skills/code-review-and-quality | Named structural remedies in review reports (supersedes C-011, was deferred) | adopted (steer) | `templates/ts-base/.claude/agents/auditor.md` (ADR 20) |
| C-072 | 2026-07-17 | agent-skills/code-review-and-quality | Change sizing thresholds (~300 changed lines = split) (supersedes C-012, was deferred) | deferred: `max-lines: 300` per file stays the wired half; the added-diff size gate joins the harness-candidate queue, trigger = first monster PR | BACKLOG harness-candidates queue |
| C-073 | 2026-07-17 | agent-skills/code-review-and-quality | Dead-code hygiene: list orphans explicitly, ask before deleting (supersedes C-013, was deferred) | adopted (steer; resolves no-gutting vs no-dead-code: list + ask) | `templates/ts-base/AGENTS.md` working rules (ADR 20) |
| C-074 | 2026-07-17 | agent-skills/code-review-and-quality | Review honesty: never soften a real issue; sycophancy is a failure mode; quantify | adopted (steer; quantify was C-003) | `templates/ts-base/.claude/agents/auditor.md` (ADR 20) |
| C-075 | 2026-07-17 | agent-skills/code-review-and-quality | Upgrade workflow: green suite before AND after; review the lockfile diff (transitive graph) | adopted (steer; extends C-006) | `templates/ts-base/AGENTS.md` Dependencies (ADR 20) |
| C-076 | 2026-07-17 | agent-skills/code-review-and-quality | Disagreement hierarchy, review-speed SLAs, standalone review checklist template | rejected: team ceremony at n=1 (C-010) and duplicated prose drifts (ADR 8) — the rites are the checklist | ADR 20 |
| C-077 | 2026-07-19 | build-code-harness (patchy631/ai-engineering-hub, A. Pachaar) | Rebuild the coding-agent harness (loop, tools, orchestration) from scratch | rejected: lab spec D6 — a custom loop measures a toy; we consume the real runtime | ADR 21 |
| C-078 | 2026-07-19 | build-code-harness | Approval gate BEFORE a tool call runs, outside the model ("enforcement, not a prompt asking to be careful") | already have (stronger): PreToolUse hooks are force — C-039/C-041; independent convergence on our doctrine | ADR 21 |
| C-079 | 2026-07-19 | build-code-harness | Sandboxed VM execution (E2B) for shell/tests | deferred: candidate sandbox for Harness Lab trial isolation; trigger = local per-trial isolation proves insufficient | HARNESS-LAB-SPEC D6 context; BACKLOG (Harness Lab) |
| C-080 | 2026-07-19 | build-code-harness | Human approval of the finished answer (human_input) | already have | merge-is-human (C-050) + plan-mode exit approval (C-060) |
| C-081 | 2026-07-19 | build-code-harness | Separate planner injects a step plan per iteration | already have via the real runtime | plan mode + todo (C-060 chain) |
| C-082 | 2026-07-19 | build-code-harness | Cross-run memory via embeddings (second provider key required) | rejected: stdlib-first (C-006 spirit) — file-based agent memory + native session resume carry the need at n=1 | ADR 21 |
| C-083 | 2026-07-19 | build-code-harness | Checkpoint/resume of interrupted runs | already have | native session resume (exercised 2026-07-19, this session) |
| C-084 | 2026-07-19 | build-code-harness | Behavior rules delivered via agent role/goal/backstory prompts | already have at stronger degree: the same rules ARE layer A (read-before-edit, minimal diff, shown evidence) delivered as constitution + hooks; identity half of personas refuted by C-088 | `home/claude/CLAUDE.md` layer A |
| C-085 | 2026-07-19 | build-code-harness | Hierarchical planner/coder/tester crew with a delegating manager | rejected: same model in three costumes adds no capability and handoffs lose context; legitimate multi-agent = isolation-as-feature (auditor, ADR 6), parallelism, cross-model (C-019) | ADR 21 |
| C-086 | 2026-07-19 | build-code-harness | Planted-bug workspace + pytest 3F/2P + "fix only account.py, never edit a test" | deferred: first candidate external seed task for the Harness Lab suite; trigger = lab task-suite authoring | HARNESS-LAB-SPEC §5 |
| C-087 | 2026-07-19 | build-code-harness | Fixture-with-planted-bug as demo/eval vehicle | already have (planned): behavioral eval of skills roadmap | ADR 14 roadmap + PLAYBOOK Roadmap |
| C-088 | 2026-07-19 | Zheng, Pei et al., "Personas in System Prompts Do Not Improve Performances of LLMs" (Findings of EMNLP 2024, checked 2026-07-19) | Personas in system prompts improve objective performance | rejected: empirically refuted — 162 personas × 9 models × 2410 MMLU questions, none beats no-persona control, some hurt; best-role selection ≈ random. First externally-measured refutation in this ledger; limits: MMLU QA, open-weight ≤72B, not agentic coding | Avoid line (constitution) + PLAYBOOK anti-patterns; ADR 22 |
| C-089 | 2026-07-20 | Daniel (session incident 2026-07-20: Grok brief → spec D6 laundered API assumption) | AI-generated input is testimony not a draft: foreign AI docs go through /absorb before informing a decision; provenance and epistemic status preserved across the model boundary | adopted (steer; no wirable half — paste/IDE channel ungated, /absorb is the only mitigation) | `home/claude/CLAUDE.md` layer A item 9 + `.claude/skills/absorb/SKILL.md` widened trigger (ADR 23) |
| C-090 | 2026-07-20 | Daniel (session, Principle 2 gap: audit depended on memory) | The audit rite is offered mechanically at the completed-unit boundary, not from memory | adopted (half-force: the PreToolUse reminder fires mechanically; acting on it stays steer) | `home/bin/audit-reminder.py` + `scripts/selftest-home.sh` ± cases (ADR 24) |
| C-091 | 2026-07-20 | Daniel (session: need a minimum planning rite not dependent on him/the session flow) | A planning-session rite exists between /kickoff and /feature: objective, anchored inventory, vertical slices, decided/rejected/open log, stop with decisions | adopted (steer; v0, refined by the lab kickoff field test) | `home/skills/plan/SKILL.md` (ADR 24) |
| C-092 | 2026-07-20 | Daniel (session: "auto-audit every relevant change" ideal) | CI auto-review runs a fresh-context review on every PR | deferred: metered API contradicts the fixed-plan stance (D6); trigger = in-session audit insufficient AND credit budget approved | PLAYBOOK Layer 4 row + ADR 24 |
| C-093 | 2026-07-20 | Daniel (owner signature 2026-07-20; supersedes C-058) | Kill/continue: by ~2026-10-17 without measurable review-cost / evidence-incident reduction on a real product, the meta-repo freezes; harness work capped ~≤20% as a tax on shipping | adopted (steer; governance convention, self-exempt from the freeze) | ADR 25 + BACKLOG (harness section) |
| C-094 | 2026-07-20 | Daniel (owner signature 2026-07-20) | Steer freeze: no new quality-doctrine steer until the lab's FINDINGS v1; force / half-force / layer-A / provenance / safety / process-rite steer stay admissible | adopted (steer; governance convention, self-exempt) | ADR 25 + `.claude/skills/absorb/SKILL.md` defer clause |
| C-095 | 2026-07-29 | harness (ADR 9 tier line; row written when the widened coverage gate found the artefact unindexed) | A checklist rite exists for NON-code workflows only, with observable completion criteria, and refuses code-implementation checklists (redirects to tests and /feature) | adopted (steer; retroactive index of a shipped rite, not a new adoption — the form gate is its mechanical half) | `home/skills/checklist/SKILL.md` + `scripts/selftest-skills.sh` form gate (ADR 27) |
| C-096 | 2026-07-29 | calendar-app `apps/web/src/lib/components-registry.test.ts` (commit 28926b0b, checked 2026-07-29) | A registry's completeness is a build property, in both directions: every shipped artefact carries an entry, and every path the registry names still exists — an incomplete map is worse than none, because it manufactures failed searches | adopted (force; direction 1 widened from selftest gates to hooks, rites, template rites and template scripts — it found `home/skills/checklist/SKILL.md` unindexed on landing) | `scripts/selftest-skills.sh` check_coverage + check_executors, with 4 negative cases (ADR 27) |
| C-097 | 2026-07-29 | calendar-app registry test, 3rd assertion (checked 2026-07-29) | An exemption may not outlive the reason for it: a baseline/allowlist entry that is now covered, or gone, is a failure — and the list may not grow quietly | adopted (force; applied to the containment allowlist — count pinned at 4, both prose copies must name all four) | `scripts/selftest-home.sh` check_allowlist + 2 negative cases (ADR 27) |
| C-098 | 2026-07-29 | calendar-app `scripts/clone-budget-check.js` header (checked 2026-07-29) | A gate's constant is measured on the repo, not picked (window 8/10/12 table: 8 finds the block that motivated the gate, 10 and 12 are blind to it) | already have | constitution "shown evidence" + anchoring law; the measurement table travels in the artefact's header, not as a new rule |
| C-099 | 2026-07-29 | calendar-app session testimony 2026-07-29 (the one part with no instance) | A creation-moment hook injects the shelf inventory when a new component is written (half-force: the reminder is mechanical, obeying it stays steer) | deferred: new mechanism, no instance exists anywhere yet; trigger = C-096/C-097 green AND one recorded duplication the registry gate did not catch, then `/decide` before any code | BACKLOG `harness-candidate` queue (ADR 18/27) |
| C-100 | 2026-07-29 | calendar-app `scripts/clone-budget-check.js` (commit 28926b0b, checked 2026-07-29) | The reuse-scan rule stops being steer by counting the result: an 8-significant-line block appearing twice is a clone, the count is a budget, and the budget may only fall | adopted (force; inside `verify`, so CI enforces it, not only the local hook) | `templates/ts-base/scripts/clone-budget-check.mjs` + `templates/ts-base/.clonebudget.json` + `scripts/selftest.sh` Claim 5 (ADR 27) |
| C-101 | 2026-07-29 | calendar-app clone-budget header (checked 2026-07-29) | The budget is a declaration, not a ban: deliberate duplication stays allowed and is raised in the same commit, because the wrong abstraction costs more than the duplication it replaced | adopted (force; the escape hatch IS the gate's contract — it ships at 0 in a fresh scaffold, so it is the zero-warnings rule with a named hatch, not a warning budget) | `templates/ts-base/.clonebudget.json` note + the script's header (ADR 27) |
| C-102 | 2026-07-29 | calendar-app clone-budget header (checked 2026-07-29) | Ratchet asymmetry: over budget fails the build, under budget prints the new number and passes — failing a commit because a refactor REMOVED clones teaches people to hate the gate | adopted (force) | `templates/ts-base/scripts/clone-budget-check.mjs` enforce() + `scripts/selftest.sh` Claim 5 asserts both directions (ADR 27) |
| C-103 | 2026-07-29 | calendar-app clone-budget header (checked 2026-07-29) | A named-exemption list gets no regeneration script (a one-command regenerator turns an allow-list into a rubber stamp); a numeric budget may have one, since the number carries no reasons to launder | adopted (force; the split is what makes both work — `--update` exists for the count, `check_allowlist` has no regenerator) | the script's `--update` vs `scripts/selftest-home.sh` check_allowlist (ADR 27) |
| C-104 | 2026-07-29 | calendar-app clone-budget `touchedFiles()` (checked 2026-07-29) | A gate's failure output is scoped to the files the author touched, not the repo's largest offenders, which they did not write and cannot act on | adopted (force; two defects fixed in the port — git's stderr is ignored so a non-repo does not bury the message under 100 lines of usage, and each git command is tried on its own so `diff HEAD` failing in a repo with no commits, i.e. commit #1, does not void the scoping) | `templates/ts-base/scripts/clone-budget-check.mjs` gitLines/touchedFiles + `scripts/selftest.sh` Claim 5 asserts the cloned file is named (ADR 27) |
| C-105 | 2026-07-30 | harness (audit 2026-07-30: the coverage gate went path-exact and these artefacts had no anchored row) | The deletion guard ships as a template script wired into the pre-commit chain, so a project gets it by copying the folder, not by remembering to write it | adopted (force; restates C-036 with the artefact anchored — C-036 anchors only the gate that proves it) | `templates/ts-base/scripts/deletion-guard.mjs` + `templates/ts-base/.husky/pre-commit` + `scripts/selftest.sh` Claim 3 (ADR 28) |
| C-106 | 2026-07-30 | harness (audit 2026-07-30, as C-105) | The lint cage ships as a flat typed ESLint config in the template, so the rules travel with the scaffold rather than being reassembled per project | adopted (force; restates C-037 with the artefact anchored) | `templates/ts-base/eslint.config.mjs` + `scripts/selftest.sh` Claim 4 (ADR 28) |
| C-107 | 2026-07-30 | harness (audit 2026-07-30, as C-105) | The Vue layer ships its own ESLint config on top of ts-base, and the framework gate is proven by consuming it exactly as its README instructs | adopted (force; restates C-038 with the artefact anchored) | `templates/vue-starter/eslint.config.ts` + `scripts/selftest-vue.sh` (ADR 28) |
| C-108 | 2026-07-30 | harness (audit 2026-07-30, as C-105) | The machine layer's hooks and permission baseline live in one tracked settings file that bootstrap symlinks into `~/.claude/`, so the cage travels with the machine | adopted (force; restates C-042 with the artefact anchored. Honest limit recorded the same day: the file also holds volatile session preference — `model`, `effortLevel` — so the symlink dirties the tree on every UI toggle; BACKLOG'd for `/decide`) | `home/claude/settings.json` + `home/bootstrap.sh` + `scripts/selftest-home.sh` wiring section (ADR 28) |
| C-109 | 2026-07-30 | harness (audit 2026-07-30, as C-105) | This repo runs itself in plan mode by default, so deliberation work cannot start with a diff | adopted (force; the mode is set by config, not by the agent's restraint) | `.claude/settings.json` `defaultMode: plan` + `scripts/selftest-skills.sh` coverage (ADR 19/28) |
| C-110 | 2026-07-30 | harness (audit 2026-07-30; supersedes C-101's anchor) | The copy-paste budget is a declaration, not a ban: deliberate duplication is raised in the same commit, and the escape hatch is part of the gate's contract | adopted (force; C-101 recorded the same claim but anchored it to a config file and a header comment — prose cannot execute, so the row promised force it could not deliver) | `templates/ts-base/scripts/clone-budget-check.mjs` enforce() + `scripts/selftest.sh` Claim 5 (ADR 28) |
| C-111 | 2026-07-30 | harness (audit 2026-07-30: the combined file crossed the template's own max-lines cap) | Clone measurement is a module separate from budget policy, and the detector is deterministic — sorted walk, locale-independent total order — so the same source cannot count differently on two machines | adopted (force; the split was forced by `max-lines: 300` running on the tooling, not chosen for taste) | `templates/ts-base/scripts/clone-detect.mjs` + `scripts/selftest.sh` Claim 1 lints it and Claim 5 exercises it (ADR 28) |
| C-112 | 2026-08-04 | harness (probes on this machine 2026-08-04, ADR 29) | Denylist-first is expressible with the product's own gate: under `bypassPermissions` both PreToolUse hooks and `deny` permission rules still fire, so "block the no-go, run the rest unasked" needs no custom mechanism; Bash itself remains uncontained (writes to $HOME and sibling projects, reads ~/.ssh, has network) and only a container bounds that | deferred: containment is the unstarted half and the only thing that bounds the shell; turning the bypass mode on before it exists was considered and rejected in ADR 29, since it removes the last control on the uncontained surface; trigger = a containment spike lands | ADR 29 + BACKLOG (harness) |
| C-113 | 2026-08-04 | harness (three unverified recommendations in one session, 2026-08-04) | A recommendation declares what verified it, or carries an honest label saying it did not; the anchoring law is not scoped to repositories | adopted (half-force: a Stop hook reads the answer and blocks once when a recommendation carries no declaration; the truth of the declaration stays steer) | `home/bin/recommendation-anchor.py` + `scripts/selftest-home.sh` ± cases (ADR 30) |
| C-114 | 2026-08-04 | harness (C-099 built on owner override; design settled by 5 probe runs 2026-08-04) | Creating a NEW file on a shared shelf stops for the human and hands the agent the shelf inventory plus its path, so "does this already exist?" stops depending on the agent remembering to look | adopted (half-force: the pause and the inventory are mechanical; recognising that Btn is Button stays judgment) | `home/bin/shelf-inventory.py` + `scripts/selftest-home.sh` 1 positive and 5 silence cases (ADR 31) |
| C-115 | 2026-08-04 | harness (owner: "não posso depender da minha memória", 2026-08-04; supersedes C-114's config prerequisite) | A shelf is detected from measured signals, not declared: 10+ files AND 10+ distinct importing directories, with `.claude/shelf.json` as override rather than prerequisite | adopted (half-force; fails closed where imports are unreadable, so a non-JS project is inert rather than noisy) | `home/bin/shelf-inventory.py` is_shelf/importer_directories + `scripts/selftest-home.sh` fan-in cases (ADR 31) |
| C-116 | 2026-08-05 | harness (owner: live coding exercise on React or Next, 2 days out) | Layer 2 React ships as a consumable overlay on ts-base: hooks rules as errors, a pure-core import ban, and the whole ts-base validity set, proven by consuming create-vite exactly as the README instructs | adopted (force) | `templates/react-starter/eslint.config.mjs` + `templates/react-starter/README.md` + `scripts/selftest-react.sh` in CI, 8 planted violations each seen rejected (ADR 32) |
| C-117 | 2026-08-05 | harness (owner override of the ADR 25 steer freeze, recorded) | React and Next conventions the linter cannot catch: no effect for derived state, server state in a query library, `"use client"` is a boundary pushed down not up, route handlers are trust boundaries | adopted (steer; quality doctrine, so the ADR 25 freeze is broken here by explicit owner decision, not by drift) | `templates/react-starter/react-conventions.md` + PLAYBOOK Layer 2 (ADR 32) |
| C-118 | 2026-08-05 | `@next/eslint-plugin-next` core-web-vitals (inspected 2026-08-05) | Next's own rule set applies with every rule pinned to error, since the harness has no warning level | adopted (force in a consuming project; NOT covered by a selftest job — the Vite path is proven end to end, the Next overlay is verified only as far as the plugin's config shape) | `templates/react-starter/eslint.next.mjs` (ADR 32) |
| C-119 | 2026-08-05 | R. C. Martin, @unclebobmartin on X, 2026-07-26 (checked 2026-08-05) | The time freed by fast generation reallocates to verification rather than to more features | already have | PLAYBOOK.md:416-420 feature loop (negative tests before production code, evidence gate for "done") |
| C-120 | 2026-08-05 | R. C. Martin, X, 2026-07-26 | Property-based and torture tests as named practices in the loop | deferred: no recorded pain and no consumer, and ADR 18 makes the harness-candidate queue the sole growth source; trigger = a defect escapes in a pure `src/lib` module that a property test would have caught | BACKLOG (harness) |
| C-121 | 2026-08-05 | R. C. Martin, X, 2026-07-26 | Mutation testing as part of the expanded test budget | deferred, and it is the only item in his list that measures test POWER rather than volume, which is the honest core of the post; already queued from the 2026-07-27 dogfood finding, scoped to an allowlist since STRATEGY-BRIEF:211 rejects it globally | BACKLOG:1064-1067 (harness) |
| C-122 | 2026-08-05 | R. C. Martin, X, 2026-07-26 | With agents you stay many times more productive and the result is better | rejected: unfalsifiable as stated, no measurement and no counter-condition; loses to C-093 (ADR 25), the kill/continue rule that refuses this exact assumption about the harness itself | ADR 33 |
| C-123 | 2026-08-05 | R. C. Martin, X, 2026-07-26 (supersedes C-053, whose trigger has since fired) | A declared dependency architecture is enforced mechanically, not left optional | adopted (force; partial by design — one declared boundary, the pure core, not a general layer map) | `templates/vue-starter/eslint.config.ts` + `templates/react-starter/eslint.config.mjs` no-restricted-imports, negative-tested in `scripts/selftest-vue.sh` and `scripts/selftest-react.sh` (ADR 33) |
| C-124 | 2026-08-05 | R. C. Martin, X, 2026-07-26 | A general declared layer map beyond the pure-core boundary | already have (the decision, and it is "skip"): PLAYBOOK.md:177 scopes arch-boundary tooling to a large codebase with layers and many contributors, and keeps only the pure-core rule for a small app | PLAYBOOK.md:177 use-when/skip-when row |
| C-125 | 2026-08-05 | R. C. Martin, X, 2026-07-26 | Tooling that identifies dependency cycles | already have | `import-x/no-cycle` as an error in all three templates; C-037 records it seen firing |
| C-126 | 2026-08-05 | R. C. Martin, X, 2026-07-26 | A UML architecture viewer to drill through modules and spot cycles visually | rejected: it informs without blocking, duplicating what no-cycle already enforces; loses to force-over-steer and to the PLAYBOOK.md:440 anti-pattern (documented-but-unwired governance is a prayer) | ADR 33 |
| C-127 | 2026-08-05 | R. C. Martin, X, 2026-07-26 (implicit in "I had my agents build the tool") | A gate written by the agent it constrains must be seen rejecting a planted violation, or it encodes the same misunderstanding and reports green | already have, with scar tissue | AGENT-LOG 2026-07-10 (import-x/no-cycle shipped wired-but-blind) + the 2026-07-30 audit, where four self-written gates could pass while blind (ADR 28) |
| C-128 | 2026-08-05 | harness (C-051, deferred 2026-07-17 for lack of a mechanism; the mechanism landed with ADR 30) | A claim of completion or "it works" is blocked unless a verification command actually ran in this session; an honest "IMPLEMENTED - NOT VERIFIED" is the sanctioned escape | adopted (half-force: it proves a command ran, parsed from tool calls not prose; whether it passed or covered the claim stays steer) | `home/bin/evidence-gate.py` + `scripts/selftest-home.sh` 8 cases (ADR 34) |
| C-129 | 2026-08-05 | harness (housekeeping: C-099's deferred row outlived the build, like C-053 before it) | The creation-moment shelf hook is BUILT, not deferred — C-099's row is its 2026-07-29 snapshot and C-114/C-115 are the built claim | already have | `home/bin/shelf-inventory.py` (ADR 31); this row only closes the stale "deferred" so the next reader does not re-discover it |
| C-130 | 2026-08-05 | harness (C-072, deferred 2026-07-17; trigger "first monster PR" fired — this branch is 1354 added lines) | A branch far past ~300 added lines vs the default branch gets a split nudge; the per-file max-lines 300 cap stays the blocking half | adopted (half-force: warns, never blocks — a hard block on total diff punishes large-but-coherent work and teaches splitting by line count) | `templates/ts-base/scripts/diff-size-check.mjs` + `.husky/pre-push` + `scripts/selftest.sh` Claim 6 (ADR 34) |
| C-131 | 2026-08-05 | harness (ADR 35, owner: force the test PLAN, not just its presence) | A fresh-context, read-only test-audit rite exists — it flags tests that are missing, at the wrong level, or too weak to fail, and the authoring session responds without judging | adopted (half-force: fresh-context + reified missing-case findings are mechanical; the weak-assertion verdict waits on Phase 2 mutation, and spec-correctness stays human) | `templates/ts-base/.claude/skills/audit-tests/SKILL.md` + `scripts/selftest.sh` Claim 0b (ADR 35) |
| C-132 | 2026-08-05 | harness (ADR 35) | The test-audit runs as a read-only agent so the authoring session's bias cannot rationalize the findings away | adopted (force: read-only by construction, and selftest.sh asserts `tools: Read, Grep, Glob` for it as for the auditor) | `templates/ts-base/.claude/agents/test-auditor.md` + `scripts/selftest.sh` Claim 0b (ADR 35) |
| C-133 | 2026-08-05 | calendar-app session report on the contract-test claim (2026-08-05, testimony; ADR 23) | A guard fails in two directions and each needs its own test: deny (unauthorized blocked) AND allow (authorized not blocked); the allow direction is the silently-missing one, and no existing metric sees its absence | adopted (steer refinement of the test-auditor's lens, admitted as process-rite per ADR 35/25; the principle is verified by logic — a surviving reject-everyone mutant IS the missing-allow signal — while the report's 1-in-155 measurement stays unverified testimony) | `templates/ts-base/.claude/agents/test-auditor.md` category 1 (ADR 35) |
| C-134 | 2026-08-05 | calendar-app report (2026-08-05) | "Every decorator key needs >=1 positive-direction assertion" as a mechanical gate; and the reachability matrix (template x endpoint) as a golden snapshot | deferred: project-specific in form (decorator name, duplicated catalog, per-controller guard stack), so template+selftest not drop-in; trigger = the calendar-app harness-candidate queue authors it | calendar-app BACKLOG (harness-candidate) |
| C-135 | 2026-08-05 | calendar-app report (2026-08-05) | Each "you cannot force this" layer has a computable half; what remains after wiring it is prioritization under cost, not knowledge of what is right | already have (sharpens, does not add): ADR 35 honest limit already says spec-correctness stays human; this widens the computable half, it is not a new mechanism | ADR 35 declared limit |
| C-136 | 2026-08-05 | calendar-app /audit-tests report (2026-08-05, testimony; supersedes the C-133 measurement anchor) | The "1 allow-assertion for 155 rules" figure was too precise: 155 rule sites reproduce exactly, but a naive count gives 12 raw 200-assertions and the genuine allow-direction count is between 1 and 12, unread | adopted (steer refinement of the test-auditor lens; the bare figure became the measured range) | `templates/ts-base/.claude/agents/test-auditor.md` category 1 (ADR 37) |
| C-137 | 2026-08-05 | calendar-app report H2 (2026-08-05) | Reify differs by case: a missing test over BROKEN code goes red; a missing test over CORRECT code (the brownfield norm) passes on first run, so it needs a named mutation and stays open for Phase 2 — never marked resolved on a green run | adopted (half-force: the triage split is mechanical in the rite; closing it still waits on Phase 2 mutation) | `templates/ts-base/.claude/skills/audit-tests/SKILL.md` step 2 (ADR 37) |
| C-138 | 2026-08-05 | calendar-app report H5 (2026-08-05) | A permission that grants a bounded view has a THIRD direction beyond deny/allow: what the holder is scoped to SEE; it fails as a silent cross-member data exposure, not a 403, and neither deny nor allow tests pin it | adopted (steer refinement of the test-auditor lens, admitted as process-rite per ADR 35/25; verified by logic and by two real exposures the 403-rich suite missed) | `templates/ts-base/.claude/agents/test-auditor.md` category 1 (ADR 37) |
| C-139 | 2026-08-05 | calendar-app report H1 (2026-08-05) | An agent copied mid-session registers only after a restart, so a skill whose step 1 launches that agent fails on its first invocation in any adopting repo, tempting an undeclared generic-subagent substitution | deferred: harness-mechanics claim about Claude Code, plausible but unverified here; trigger = the next brownfield adoption or /kickoff, where the copy step gains a restart note or the skill names and logs the fallback | BACKLOG (harness) |
| C-140 | 2026-08-05 | calendar-app report H3 (2026-08-05) | A consuming repo may have no AGENT-LOG.md while the audit rites write to it by name, so brownfield pain never reaches the ADR 18 growth queue | deferred: the PLAYBOOK brownfield path names AGENTS/DECISIONS/danger-zones but not AGENT-LOG; trigger = next brownfield adoption adds AGENT-LOG creation to the day-one gate list | BACKLOG (harness) + PLAYBOOK.md:357-411 |
| C-141 | 2026-08-05 | calendar-app report H4 (2026-08-05) | ADR 9 stamps copied skills with source: agentic-harness@sha but not copied agents, so a vendored agent drifts unstamped | deferred: owner design question — extend the stamp to `.claude/agents/*.md`, or pin agents another way; trigger = owner decides | BACKLOG (harness) + ADR 9 |
| C-142 | 2026-08-05 | calendar-app /audit-tests first real run (2026-08-05) | The rite's first real consumption produced 14 findings, 5 Critical, 0 confabulated — the calibration number for trusting test-auditor, caveated that 4 of 14 rest on the auditor's word alone | already have (calibration datum; logged in calendar-app AGENT-LOG) | calendar-app `AGENT-LOG.md` + this row |
| C-143 | 2026-08-05 | harness (Principle 1: reify what can be reified; the rule was prayer) | Mutation testing reifies "every test must fail if the logic breaks" — Stryker mutates src/lib and fails if any mutant survives, opt-in and on-demand (not in verify), allowlisted to the pure core | adopted (force; seen rejecting a real weak test that never pins a boundary, and passing a strong one, in `scripts/selftest.sh` Claim 7) | `templates/ts-base/stryker.config.mjs` + `pnpm mutants` + `scripts/selftest.sh` Claim 7 (ADR 38) |
| C-144 | 2026-08-06 | harness (owner: certain growth in a build-once template is not YAGNI; ADR 39) | Mutation testing runs as a blocking CI job on every push and PR, incremental and cached so cost tracks the diff not the codebase, failing safe to a full run on a cache miss | adopted (force; the job ships in ts-base ci.yml, and selftest.sh asserts the job, the incremental script, and the cache are wired) | `templates/ts-base/.github/workflows/ci.yml` mutation job + `package.snippet.json` mutants:ci + `scripts/selftest.sh` Claim 7 (ADR 39) |
| C-145 | 2026-08-06 | harness (owner: review does not scale against AI volume; force the separation, not review it) | A module-scope pure function in a .tsx renderer is counted, and the count may only fall — the wired half of "components render, lib decides", modeled on the clone budget with the same baseline-absorbs-noise contract | adopted (force; ships at 0 in verify, seen rejecting a real stranded function and passing when it moves to src/lib, with the canvas false-positive class skipped) | `templates/ts-base/scripts/stranded-logic-check.mjs` + `.strandedbudget.json` + `scripts/selftest.sh` Claim 8 (ADR 40) |
| C-146 | 2026-08-06 | harness (owner: doc rot is the Kinous failure; fix it AND mechanize against it) | The docs that enumerate the verify contents must name every step the template's verify actually runs; a step added and undocumented fails CI, turning silent doc rot into a red build | adopted (force; seen rejecting a planted doc that omits a real verify step, in `scripts/selftest-skills.sh`) | `scripts/selftest-skills.sh` check_verify_docs + negative case (ADR 41) |
| C-147 | 2026-08-06 | skills api-and-interface-design + frontend-ui-engineering (imported catalog, testimony ADR 23) | The React path forces no accessibility linter while the Vue path does (eslint-plugin-vuejs-accessibility) | adopted (force; eslint-plugin-jsx-a11y in react-starter — its recommended set plus alt-text pinned to error, seen rejecting an image without alt in selftest-react.sh) | `templates/react-starter/eslint.config.mjs` + `package.snippet.json` + `scripts/selftest-react.sh` (ADR 42) |
| C-148 | 2026-08-06 | frontend-ui-engineering skill (imported catalog, testimony ADR 23) | Avoid the "AI aesthetic" (default purple/gradients/max-rounding/oversized padding) as an AI-specific quality lever no linter catches | deferred (quality-doctrine steer, ADR 25 freeze -> post-lab queue); highest-value un-forceable kernel of the lot, and its forceable slice (ban raw hex / arbitrary pixel values) is a separate force candidate if a real UI project earns it | BACKLOG (post-lab steer queue) |
| C-149 | 2026-08-06 | api-and-interface-design skill (imported catalog, testimony ADR 23) | Hyrum's Law: every observable behavior becomes a de facto contract, so be intentional about what is exposed | deferred (quality-doctrine steer, ADR 25 freeze -> post-lab queue); pure judgment, no forceable slice | BACKLOG (post-lab steer queue) |
| C-150 | 2026-08-06 | skills sweep (api-and-interface-design + frontend-ui-engineering) | The remaining ~25 claims across both skills | already have or rejected: validation / error-shapes / pagination map to the Layer 3 surface packs; component size / switch-exhaustiveness / prop-drilling to forced eslint rules and PLAYBOOK conventions (C-117 for React state); the self-answered "Verification" checklists are ADR-14 decoration. Lot yield 1 non-frozen adoption < the sweep's 2-adoption floor: catalog absorption ends as a routine | this row + C-147..C-149 (ADR 42) |
| C-151 | 2026-08-06 | harness (owner: a skill can be born dead; do not trust memory to activate it) | A git-tracked skill missing its ~/.claude symlink is auto-linked on SessionStart; an untracked dir in home/skills is warned, never auto-activated | adopted (force for the tracked auto-link, plus a half-force warn on untracked; seen linking a tracked skill, warning + refusing to link an untracked one, activating it once vetted, and going silent when all are linked, in `scripts/selftest-home.sh`) | `home/bin/skill-activation.py` + `home/claude/settings.json` SessionStart + `scripts/selftest-home.sh` (ADR 43) |
| C-152 | 2026-08-06 | harness (owner: the backlog injection leaks cross-project PII into isolated sessions; a live rehearsal proved it) | The SessionStart BACKLOG injection fires only at the ~/Dev desk (cwd == ~/Dev); in any project subfolder it stays silent, so the cross-project index and its career PII never reach a project or interview session | adopted (force; seen injecting at the desk and staying silent in a project subfolder in `scripts/selftest-home.sh`) | `home/bin/backlog-inject.py` + `home/claude/settings.json` SessionStart + `scripts/selftest-home.sh` (ADR 45) |
| C-153 | 2026-08-06 | harness (owner: push-to-main is a leaky string blocklist; a rehearsal push exposed it) | A machine-layer PreToolUse hook denies any git push that reaches the default branch by resolving the actual target (an explicit default-branch ref in any form, or a current-branch push while HEAD is default), asks when the target is unresolvable, and allows a clear work-branch push | adopted (force; seen denying every leaked form — bare push on main, -u origin main, +main, HEAD:main, origin HEAD, feature:master — and allowing work-branch pushes, in `scripts/selftest-home.sh`) | `home/bin/push-guard.py` + `home/claude/settings.json` PreToolUse Bash + `scripts/selftest-home.sh` (ADR 46) |
| C-154 | 2026-08-06 | harness (owner: the greenfield/brownfield fork was unrouted, exposed while explaining the kickoff flow) | Project entry is split into a neutral /start router that asks the human green-or-brown before any scaffold or edit and routes, plus /greenfield (renamed from /kickoff) and /brownfield (wraps the PLAYBOOK BROWNFIELD checklist), so the posture-inverting fork is decided first and explicitly instead of by which path the human happens to invoke | adopted (steer; the router asks but a determined agent can bypass it, so a hard-force SessionStart hook stays deferred; process-rite, not quality-doctrine, so outside the ADR 25 freeze) | `.claude/skills/start/SKILL.md` + `.claude/skills/greenfield/SKILL.md` + `.claude/skills/brownfield/SKILL.md` + `PLAYBOOK.md` entry note (ADR 49) |
| C-155 | 2026-08-07 | harness (owner: kickoff skills were unreachable from a fresh folder, confirmed by a second independent session) | The project-birth skills /start, /greenfield, /brownfield are user-tier (in home/skills, symlinked to ~/.claude/skills by bootstrap), not harness-project-scoped, because they must be invocable before the new project has any skills of its own; /absorb stays project-scoped as it edits the harness's own ledger | adopted (steer; supersedes C-154 location anchor; a gate asserting kickoff skills are user-tier is a deferred follow-up) | `home/skills/start/SKILL.md` + `home/skills/greenfield/SKILL.md` + `home/skills/brownfield/SKILL.md` + `home/bootstrap.sh` (ADR 50) |
| C-156 | 2026-08-07 | harness (owner: target role is Next.js/Supabase/Tailwind; confirmed imminent, C-144 not YAGNI) | A gated Next.js 16 Layer-2 template (next-starter) overlays create-next-app: strictTypeChecked + ts-base validity rules + eslint-config-next with --max-warnings 0, the pure-core ban extended to next, and ts-base tsconfig strictness on Next's own | adopted (force; scripts/selftest-next.sh consumes the template as its README instructs and is seen rejecting nine lint rules + the next/* pure-core ban + a clone + stranded .tsx logic, wired as a blocking CI job) | `scripts/selftest-next.sh` + `templates/next-starter/eslint.config.mjs` + `.github/workflows/selftest.yml` (ADR 51) |
| C-157 | 2026-08-07 | harness (owner: a live exercise showed the greenfield flow reaching layer selection before the human's design decisions) | The greenfield spec interview asks Layer 3 by EFFECT not noun (idempotency, critical mutation, atomicity), and the kickoff routes non-trivial features through /plan before any interface is written (inline for a single-feature project), so human-owned design decisions precede mechanical commitment | adopted (steer; a better prompt at the skip point plus a routing rite - whether the human decided is semantic and stays the human's replica, not forceable) | `home/skills/greenfield/SKILL.md` (ADR 52) |
| C-158 | 2026-08-07 | harness (exercise-3 setup: a fresh clone of a real Next 16 repo failed verify on LayoutProps) | The next-starter typecheck runs `next typegen && tsc --noEmit` so code using Next's generated types (LayoutProps/PageProps) verifies on a fresh clone or CI, not only after a manual build; the selftest scaffolds a layout using the generated type and asserts a bare tsc fails without it | adopted (force; scripts/selftest-next.sh is seen failing bare tsc on the missing generated type and passing the typegen-first script, wired in CI) | `scripts/selftest-next.sh` + `templates/next-starter/package.snippet.json` (ADR 53) |
| C-159 | 2026-08-07 | harness (owner: onboarding a real codebase needs deeper, personal understanding than /brownfield's change-scoped archaeology) | A universal /onboard rite produces a personal, git-excluded onboarding map in seven sections (purpose, structure, conventions, health and history, character, harness-comparison, questions-for-the-team), reading deeply without optimising for tokens or time; distinct from /brownfield (enter to change) and /audit (hunt bugs) | adopted (steer; a comprehension rite whose depth is the human's replica, the reifiable half being only the form gate on the skill card) | `home/skills/onboard/SKILL.md` (ADR 54) |
| C-160 | 2026-08-07 | harness (ex-3 brownfield: a pre-registered kill test was a dated prose reminder nothing ran; the concurrency defect it named shipped live past a green suite) | A skipped or focused test is a hole in a green suite, so ts-base eslint bans .only (no-focused-tests) and requires a .skip/.todo to carry an eslint-disable with a reason (no-disabled-tests), keeping a pre-registered kill test visible; and /greenfield + /feature require the riskiest-assumption kill test to be a named test (red or skip-with-reason), never a dated line | adopted (force; the eslint rules seen rejecting a planted .only and a bare .skip in scripts/selftest.sh Claim 4b, plus the steer card change that the kill test is a test not a date) | `templates/ts-base/eslint.config.mjs` + `scripts/selftest.sh` + `home/skills/greenfield/SKILL.md` (ADR 58) |
| C-161 | 2026-08-07 | ex-3 /audit-tests (16 mutants survived src/components and src/app, then died after the reified tests, a ~90s named-list probe) | /audit-tests Phase 2 is a named-list mutation probe: a weak-assertion or missing-test finding is closed only when the auditor's named mutation, applied to production code, DIES against the reified test; a survivor keeps it open. It reaches code Stryker's src/lib scope does not (UI), refuting ADR 38's "UI is least reward" without full-Stryker cost | adopted (steer; the probe is the rite's Phase 2 mechanism, proven on ex-3 at 16 survived to 16 killed, ad-hoc per finding not a shipped script) | `templates/ts-base/.claude/skills/audit-tests/SKILL.md` (ADR 59) |
