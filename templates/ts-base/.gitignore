node_modules/
dist/
coverage/
*.local

# Secrets — never committed; the agent is also read-blocked on these
.env
.env.*

# Private — personal learning / interview prep (never `git add -f`)
/notes/
INTERVIEW-NOTES.md

# Mutation testing (Stryker)
reports/
.stryker-tmp/
